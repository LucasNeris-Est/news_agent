# Google API Key (obrigatório)
# Obtenha em: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=sua-chave-google-aqui

# PostgreSQL Connection Strings
# NOTA: Se usar Docker Compose, essas variáveis são configuradas automaticamente
# e você só precisa definir GOOGLE_API_KEY e LLM_MODEL (opcional)

# Banco Vetorial (com pgvector) - usado para RAG
# Para uso com Docker Compose: não precisa definir (configurado automaticamente)
# Para uso local: postgresql://user:password@localhost:5432/news_db
POSTGRES_VECTOR_DB_CONNECTION_STRING=postgresql://news_agent:news_agent_password@localhost:5433/news_agent_db

# Banco Normal (PostgreSQL padrão) - usado para cache de análises
# Para uso com Docker Compose: não precisa definir (configurado automaticamente)
# Para uso local: postgresql://user:password@localhost:5432/news_cache_db
POSTGRES_CACHE_DB_CONNECTION_STRING=postgresql://user:password@localhost:5432/news_cache_db

# Opcional: Modelo LLM a ser usado
LLM_MODEL=gemini-2.5-flash

# Opcional: Modelo de embeddings (padrão: BAAI/bge-m3)
# EMBEDDING_MODEL=BAAI/bge-m3

